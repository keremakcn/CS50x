#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int height;
    do
    {
        height = get_int("Do you want how many stepness: ");
    }

    for (int i = 0; text[i] != '\0'; i++)
    {

    }
}
